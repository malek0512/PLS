#include<stdio.h>
#include<stdlib.h>

maillon* readFromFile(char *nom){
    FILE data;
    data = fopen(nom, "r+");
    
    char c;
    do while (c != EOF){
        c = getchar(f);
        
    }

}
